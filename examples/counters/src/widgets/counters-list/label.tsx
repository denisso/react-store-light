import { Container } from '../../shared/ui/layout';
import { Label } from '../../shared/ui/label';

export const Buttons = () => {
  return (
    <Container className='flex gap-2'>
      <Label>List counters:</Label>
    </Container>
  );
};
